/** 1.- Crear una instancia nueva de un objeto:
 * let p =new persona("Pedro")
 */

/** 2.- Cual no es una opción a la hora de poner un comentario:
 * !laksñldklñas
 */

/** 3.- Palabra que se usa para generar un error:
 * throw
 */

/** 4.- Operador que seleccionarías cuando dos valores NO son iguales
 * !==
 */

/** 5.- de las siguientes opciones cual devolvería TRUE
 * 3=="3"
 */

/** 6.- Disponemos de una lista de estados let estados = ["Completa","incompleta","por definir"], si queremos obtener completa deberíamos usar.... 
 * estados[0]
*/

/**7.- Si queremos iterar por cada una de las propiedades de un objeto debemos usar...
 * object.keys()
 */

/**8.- método que se emplearía para cancelar desde un evento el funcionamiento por defecto sería...
 * e.prevent.DEfault()
 */

/** 9.- Cual no sería una palabra reservada en JAvaScript
 * array
 */

/** 10.- la salida que obtendríamos al ejecutar 0 && "Hola ->Errada
 * 0
 */

/** 11.- para que se emplea la funcion fetch()...
 * Para realizar peticiones http
 */

/**12.- diferencia entre colecciones creadas con Map y Object -> Errada
 *  puedes iterar en un mapa por orden de insercion
 */

/**13.- Ejecución que puede ser suspendida y se puede recuperar mas adelante
 *  función asincrona (async/ awat)
 */

/** 14.- diferencia entre for y foreach
 *  FOR es genérico, FOREACH solo con arrays
 */

/** 15.- Para que se utiliza la funcion .them()
 * para resolver un código cuando una promesa se cumple
 */

/**16.- opción incorrecta a la hora de definir una función flecha que devuelva un objeto vacio -> Errada
 * a = () => {}
*/

/**17.- definición correcta de una función flecha
 * (a,b) =>b
 */

/**18.- si tratamos de ejecutar un valor como si fuera una función, que error devuelve
 * typeError 
 */

/**19.- colección que permite valores únicos -errada
 * Set
 */

/**20.- si se ejecuta por consola: console.log(typeof(100))....se obtiene;
 * Nomber
 */

console.log(typeof(100))